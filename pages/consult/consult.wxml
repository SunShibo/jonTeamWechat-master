<view class="html">
<view class="body">
  	<view class="main">
  		<view class="head">
  			<view>
  				行业资讯
  			</view>
  		</view>
  		<view class="block" bindtap="goConsultPar" data-id="{{item.id}}" wx:for="{{list}}"
			  wx:key="index">
  			<image class="imgOne" src="{{item.image}}"></image>
  			<view>
  				<view class="fontOne">{{item.title}}</view>
  				<view class="fontTwo">{{item.introduction}}</view>
				<view class="out">
					<view class="fontThree">{{item.updateTime}}</view>
					<view class="fontFour">{{item.source}}</view>
				</view>
  			</view>
  		</view>
  	</view>
  </view>

</view>